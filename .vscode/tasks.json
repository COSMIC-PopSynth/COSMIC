{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "make test",
            "options": {
                "cwd": "${workspaceFolder}/src/cosmic/src"
            },
        }
    ]
}