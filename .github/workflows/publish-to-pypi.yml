name: Publish Python 🐍 distributions 📦 to PyPI

on: push

jobs:
  build-code-mac:
    name: Build and publish Python 🐍 distributions 📦 to PyPI and TestPyPI
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
        python-version: [3.6, 3.7, 3.8]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install GFortran macOS
      run: brew install gcc

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python3 -m pip install --upgrade setuptools wheel
        python3 -m pip install --upgrade twine
        python3 -m pip install numpy

    - name: Build a binary wheel and a source tarball
      run: |
        git tag v3.0.1d
        NPY_DISTUTILS_APPEND_FLAGS=1 LDFLAGS='-static -static-libgfortran /usr/local/opt/gcc/lib/gcc/9/libquadmath.a' python3 setup.py sdist bdist_wheel

    - name: Upload builds
      uses: actions/upload-artifact@v1
      with:
        name: dist
        path: dist

#  build-code-linux:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v2
#    - name: Set up Python
#      uses: actions/setup-python@v1
#      with:
#        python-version: 3.8
#    - name: Install dependencies
#      run: |
#        python -m pip install --upgrade pip
#        pip install twine flake8
#    - name: Build manylinux Python wheels
#      uses: RalfG/python-wheels-manylinux-build@v0.2.2-manylinux2010_x86_64
#      with:
#        build-requirements: 'numpy'
#        system-packages: 'hdf5 gcc-gfortran'
#        package-path: ''
#        pip-wheel-args: '--no-deps'
#    - name: Publish wheels to PyPI
#      env:
#        TWINE_USERNAME: ${{ secrets.test_pypi_password }}
#        TWINE_PASSWORD: ${{ secrets.test_pypi_password }}
#      run: |
#        twine upload wheelhouse/*-manylinux*.whl

  publish-mac:
    needs: [build-code-mac]
    name: Publish Python 🐍 distributions 📦 to PyPI and TestPyPI
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.8]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python3 -m pip install --upgrade setuptools wheel
        python3 -m pip install --upgrade twine

    - name: Download builds
      uses: actions/download-artifact@v1
      with:
          name: dist

    - name: Publish distribution 📦 to Test PyPI
      uses: pypa/gh-action-pypi-publish@master
      with:
        password: ${{ secrets.test_pypi_password }}
        repository_url: https://test.pypi.org/legacy/

    - name: Publish distribution 📦 to PyPI
      if: startsWith(github.event.ref, 'refs/tags')
      uses: pypa/gh-action-pypi-publish@master
      with:
        password: ${{ secrets.pypi_password }}
